source: local_directory
path: data/images # populated by `dvc pull`; tracked via data/images.dvc
external_data_size: 10000 # number of images in the dataset
train_size: 8000 # images used for training, the rest are used for validation
image_size_1: 256
image_size_2: 256
batch_size: 16
num_workers: 0
pin_memory: true
