apiVersion: batch/v1
kind: Job
metadata:
  name: trainer
  namespace: colorflow
spec:
  template:
    spec:
      containers:
        - name: trainer
          env:
            - name: DVC_PULL_DATA
              value: "false"
            - name: GCS_DATA_URI
              value: "gs://mlops-coco/images"
